# packages/aegis-core — shared Python lib

Settings, structured logging, OpenTelemetry tracing, **tenant-context** contextvar,
shared Pydantic models, error types, secrets abstraction. Imported by every Python service.

Scaffolded by task **INFRA-03** (see [`/TODO.md`](../../TODO.md)).
